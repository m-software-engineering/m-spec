# M-SPEC Change Artifacts

Each change gets its own directory with planning, delta specs, and optimize-session artifacts.
Recommended layout:

```text
{{CHANGES_ROOT}}/<change-slug>/
  plan.md
  specs/
  optimize/
```
