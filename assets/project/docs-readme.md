# M-SPEC

Primary agent: `{{AGENT}}`

## Workflow

1. `plan` creates a detailed `plan.md` for a specific change.
2. `spec` expands the plan into parallelizable Gherkin specs.
3. `implement` executes via agent-native delegation and strict TDD.
4. `optimize` runs an autoresearch ratchet loop on a constrained scope.

## Domain roots

{{DOMAIN_LIST}}
