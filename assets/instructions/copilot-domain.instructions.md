---
applyTo: "{{DOMAIN_APPLY_TO}}"
---

Focus on the {{DOMAIN_NAME}} domain rooted at `{{DOMAIN_PATH}}`.
Reuse the local conventions in that subtree before introducing new abstractions.
If work spans multiple domains, document the boundary explicitly in the active M-SPEC plan.
