## Domain focus: {{DOMAIN_NAME}}

- Treat `{{DOMAIN_PATH}}` as a bounded context with its own rules and vocabulary.
- Do not leak assumptions from unrelated domains into this area.
- Prefer local conventions, tests, and existing abstractions before introducing new ones.
- If you change this domain, update the relevant M-SPEC plan and slice specs first.
