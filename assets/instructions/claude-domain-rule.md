# Domain rule: {{DOMAIN_NAME}}

Applies to work centered on `{{DOMAIN_PATH}}`.

- Stay inside this domain unless the plan explicitly requires cross-domain edits.
- Reuse local tests, fixtures, and naming conventions first.
- Keep ownership boundaries explicit when delegating to subagents.
